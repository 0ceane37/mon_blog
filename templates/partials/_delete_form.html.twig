

<form method="post" action="{{ path('delete', {'id': article.id}) }}"
      onsubmit="return confirm('Etes-vous sûr de vouloir supprimer l article ?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">

    <button class="btn btn-primary">Supprimer <i class="fa fa-trash"></i></button>
</form>


